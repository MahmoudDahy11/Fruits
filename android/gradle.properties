# =========================
# 🔹 تحسين أداء Gradle لجهاز 8GB RAM
# =========================

# JVM heap: ندي Gradle 4GB (متوازن)
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError

# تشغيل الـ Daemon عشان التسريع في الـ builds اللي بعده
org.gradle.daemon=true

# تشغيل الـ Parallel build (يبني الموديولات مع بعض)
org.gradle.parallel=true

# تفعيل الـ Build cache
org.gradle.caching=true

# تحميل التاسكات المطلوبة فقط
org.gradle.configureondemand=true


# =========================
# 🔹 إعدادات Kotlin Compiler
# =========================

# تفعيل incremental build للكوتلن
kotlin.incremental=true

# طريقة تشغيل الكومبايلر (اختار in-process عشان ثابتة وسريعة)
kotlin.compiler.execution.strategy=in-process


# =========================
# 🔹 إعدادات Flutter / AndroidX
# =========================

android.useAndroidX=true
android.enableJetifier=true
